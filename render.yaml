services:
  - type: web
    name: bridge-automation-quiz
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    autoDeployTrigger: commit
    previews:
      generation: automatic
